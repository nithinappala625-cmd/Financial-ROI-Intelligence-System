"""Employees routes — TODO: Implement. See Section 14.2 and RBAC matrix."""
from fastapi import APIRouter
router = APIRouter(prefix="/employees", tags=["Employees"])
# TODO: GET /employees, POST /employees, GET /employees/{id}/value-score, GET /employees/{id}/productivity
