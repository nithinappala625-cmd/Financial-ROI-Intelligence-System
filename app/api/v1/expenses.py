"""Expenses routes — TODO: Implement. See Section 14.2 and RBAC matrix."""
from fastapi import APIRouter
router = APIRouter(prefix="/expenses", tags=["Expenses"])
# TODO: GET /expenses, POST /expenses, GET /expenses/summary, GET /expenses/anomalies
